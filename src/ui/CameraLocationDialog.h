#ifndef CAMERALOCATIONDIALOG_H
#define CAMERALOCATIONDIALOG_H

#include "WizardDialog.h"

class CameraLocationDialog : public QDialog
{
    Q_OBJECT
public:
    explicit CameraLocationDialog(QWidget* parent = nullptr);
    ~CameraLocationDialog();

private:

};

#endif // CAMERALOCATIONDIALOG_H